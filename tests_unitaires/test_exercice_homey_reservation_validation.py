import unittest
from exercice_homey_reservation_validation import calculer_prix_total

# Classe de tests unitaires (à compléter)
class TestCalculPrix(unittest.TestCase):
    # TODO : Ajouter les tests unitaires ici
    # 1. Tester un calcul normal : 5 nuits à 80€ (assertEqual).
    # 2. Tester avec nombre de nuits négatif (assertRaises ValueError).
    # 3. Tester avec prix par nuit négatif (assertRaises ValueError).
    # 4. Tester avec prix par nuit zéro (assertRaises ValueError).
    
    pass

if __name__ == "__main__":
    unittest.main() 